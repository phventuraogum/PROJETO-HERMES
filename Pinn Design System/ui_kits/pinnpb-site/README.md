# Pinn — Marketing Site UI Kit (`pinnpb.com` recreation)

**Status:** inferred from brandbook descriptions of the site's *intended* hero, anti-positioning blocks, ICP / Revenue OS explainer, and casos block. Live-site source was not provided. Calibrate against `pinnpb.com` before production.

## Components
- `Nav.jsx` — sticky top nav, blur backdrop
- `Hero.jsx` — contrarian-open hero, ~720px height, primary CTA "Diagnóstico de 90 min"
- `AntiPositioning.jsx` — 4-block grid of "não somos / somos"
- `RevenueOS.jsx` — five-dimension explainer (demanda · qualif · conv · expansão · governança)
- `Cases.jsx` — anonymised case cards (industria, R$, deal-cycle anchor)
- `DiagnosticoCTA.jsx` — closing CTA, calendar icon
- `Footer.jsx` — corp footer with `Revenue OS.` tagline

Open `index.html` for the click-thru.
